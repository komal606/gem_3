{
  "name": "gem-proxy",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^3.3.2",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18"
  }
}
```

---

**File 3 — `Procfile`** (no extension, just `Procfile`)
```
web: node index.js
```

---

**File 4 — `.env.example`**
```
GEM_API_KEY=gem_live_xxxxxxxxxxxxxxxxxxxx
PORT=3001
